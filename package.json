{
  "name": "planning-poker",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client",
    "functions"
  ],
  "scripts": {
    "dev": "npm run dev -w @planning-poker/client",
    "build": "npm run build -w @planning-poker/functions && npm run build -w @planning-poker/client",
    "build:client": "npm run build -w @planning-poker/client",
    "build:functions": "npm run build -w @planning-poker/functions",
    "lint": "npm run lint -w @planning-poker/client",
    "preview": "npm run preview -w @planning-poker/client",
    "deploy": "npm run build && firebase deploy",
    "deploy:hosting": "npm run build:client && firebase deploy --only hosting",
    "deploy:rules": "firebase deploy --only firestore:rules",
    "deploy:functions": "npm install --prefix functions && npm run build:functions && firebase deploy --only functions"
  }
}
